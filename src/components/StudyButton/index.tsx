import React from 'react';
import '../../styles/studyButtonStyles.scss';

class StudyButton extends React.Component {
    render() {
        return(
            <button className='studyButton'>Botão</button>
        )
    }
}

export default StudyButton;